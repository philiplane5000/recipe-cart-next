# Committed to git — local development defaults only, no production secrets
# Copy .env.local.example to .env.local for connection-string overrides
MONGODB_APP_NAME=recipe-cart
MONGODB_DB_NAME=recipecart

# Local Docker container credentials (not sensitive — dev only)
MONGODB_ROOT_USERNAME=root
MONGODB_ROOT_PASSWORD=another-password
